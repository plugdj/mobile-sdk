<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dj.plug.plugin.digitalix.example1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="pdjExtensionName" android:value="My First Extension" />
        <meta-data android:name="pdjExtensionMainClass" android:value="xyz.digitalix.ext_example.ExampleMain" />
        <meta-data android:name="pdjExtensionDescription" android:value="Description... :)" />

    </application>
</manifest>
